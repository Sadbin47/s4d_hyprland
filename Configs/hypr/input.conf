# Input configuration
input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0
    
    touchpad {
        natural_scroll = false
        tap-to-click = true
        drag_lock = 0
    }
}

# Gestures - new syntax (workspace_swipe was removed in 0.45+)
gesture = 3, horizontal, workspace
