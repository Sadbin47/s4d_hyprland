# ── User Preferences ──────────────────────────────────────────────────────────
# This file is for YOUR personal overrides. Will NOT be overwritten by updates.
# Anything here overrides defaults from other config files.
#
# Examples:
# general { gaps_in = 0; gaps_out = 0; border_size = 0 }
# decoration { rounding = 0 }
# input { kb_layout = de }
